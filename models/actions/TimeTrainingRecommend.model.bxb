action (TimeTrainingRecommend) {
  description (운동 시간대에 따른 루틴 추천)
  collect {
    input (rtime) {
      type (RTime)
      min (Optional) max (One)
    }
  }
  type (Search)
  output (TimeTrainingResults)
}
