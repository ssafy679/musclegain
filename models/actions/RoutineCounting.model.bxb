action (RoutineCounting) {
  description (운동 루틴을 카운트해주는 액션)
  collect {
    input (rName) { // 운동 루틴 이름
      type (RName)
      min (Required) max (One)
    }
  }
  type (Search)
  output (RoutineCountResults)
}
